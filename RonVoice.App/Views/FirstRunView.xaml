<Window x:Class="RonVoice.App.Views.FirstRunView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RonVoice — primeira execução" Height="230" Width="520"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <StackPanel Margin="24" VerticalAlignment="Center">

        <TextBlock Text="Preparando o RonVoice" FontSize="16" FontWeight="SemiBold" />
        <TextBlock Foreground="{StaticResource Muted}" FontSize="12" TextWrapping="Wrap" Margin="0,6,0,16"
                   Text="O reconhecimento de voz precisa de um modelo que não vem junto do programa. Isso acontece só uma vez." />

        <ProgressBar Height="20" Minimum="0" Maximum="1" Value="{Binding Progress, Mode=OneWay}" />
        <TextBlock Text="{Binding StatusText}" TextWrapping="Wrap" Margin="0,10,0,0"
                   FontSize="12" />

        <Button x:Name="RetryButton" Content="Tentar de novo" Padding="14,5" Margin="0,16,0,0"
                HorizontalAlignment="Left">
            <Button.Style>
                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Failed}" Value="True">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>

    </StackPanel>
</Window>
